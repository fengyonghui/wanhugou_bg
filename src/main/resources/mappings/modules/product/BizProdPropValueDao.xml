<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wanhutong.backend.modules.product.dao.BizProdPropValueDao">
    
	<sql id="bizProdPropValueColumns">
		a.id AS "id",
		a.prop_id AS "propId",
		a.cate_prop_id AS "catePropId",
		a.prop_name AS "propName",
		a.cat_value_id AS "catValueId",
		a.prop_value AS "propValue",
		a.source AS "source",
		a.create_id AS "createId.id",
		a.create_time AS "createTime",
		a.status AS "status",
		a.update_id AS "updateId.id",
		a.update_time AS "updateTime",
		a.u_version AS "uVersion"
	</sql>
	
	<sql id="bizProdPropValueJoins">
	</sql>
    
	<select id="get" resultType="BizProdPropValue">
		SELECT 
			<include refid="bizProdPropValueColumns"/>
		FROM biz_prod_prop_value a
		<include refid="bizProdPropValueJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="BizProdPropValue">
		SELECT 
			<include refid="bizProdPropValueColumns"/>
		FROM biz_prod_prop_value a
		<include refid="bizProdPropValueJoins"/>
		<where>
			a.status = #{DEL_FLAG_NORMAL}
			<if test="propName != null and propName != ''">
				AND a.prop_name LIKE 
					<if test="dbName == 'oracle'">'%'||#{propName}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{propName}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{propName},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="BizProdPropValue">
		SELECT 
			<include refid="bizProdPropValueColumns"/>
		FROM biz_prod_prop_value a
		<include refid="bizProdPropValueJoins"/>
		<where>
			a.status = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO biz_prod_prop_value(
			id,
			prop_id,
			cate_prop_id,
			prop_name,
			cat_value_id,
			prop_value,
			source,
			create_id,
			create_time,
			status,
			update_id,
			update_time,
			u_version
		) VALUES (
			#{id},
			#{propId},
			#{catePropId},
			#{propName},
			#{catValueId},
			#{propValue},
			#{source},
			#{createId.id},
			#{createTime},
			#{status},
			#{updateId.id},
			#{updateTime},
			#{uVersion}
		)
	</insert>
	
	<update id="update">
		UPDATE biz_prod_prop_value SET 	
			prop_id = #{propId},
			cate_prop_id = #{catePropId},
			prop_name = #{propName},
			cat_value_id = #{catValueId},
			prop_value = #{propValue},
			source = #{source},
			create_time = #{createTime},
			update_id = #{updateId.id},
			update_time = #{updateTime},
			u_version = #{uVersion}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE biz_prod_prop_value SET 
			status = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>