<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wanhutong.backend.modules.biz.dao.inventory.BizDetailInvoiceDao">
    
	<sql id="bizDetailInvoiceColumns">
		a.id AS "id",
		a.invoice_id AS "invoice",
		a.order_header_id AS "orderHeader",
		a.request_header_id AS "requestHeader"
	</sql>
	
	<sql id="bizDetailInvoiceJoins">
	</sql>
    
	<select id="get" resultType="BizDetailInvoice">
		SELECT 
			<include refid="bizDetailInvoiceColumns"/>
		FROM biz_detail_invoice a
		<include refid="bizDetailInvoiceJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="BizDetailInvoice">
		SELECT 
			<include refid="bizDetailInvoiceColumns"/>
		FROM biz_detail_invoice a
		<include refid="bizDetailInvoiceJoins"/>
		<where>
			
			<if test="invoice != null and invoice != ''">
				AND a.invoice_id = #{invoice}
			</if>
			<if test="orderHeader != null and orderHeader.id != null and orderHeader.id != ''">
				AND a.order_header_id = #{orderHeader}
			</if>
			<if test="requestHeader != null and requestHeader.id != null and requestHeader.id != ''">
				AND a.request_header_id = #{requestHeader}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="BizDetailInvoice">
		SELECT 
			<include refid="bizDetailInvoiceColumns"/>
		FROM biz_detail_invoice a
		<include refid="bizDetailInvoiceJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO biz_detail_invoice(
			id,
			invoice_id,
			order_header_id,
			request_header_id
		) VALUES (
			#{id},
			#{invoice},
			#{orderHeader},
			#{requestHeader}
		)
	</insert>
	
	<update id="update">
		UPDATE biz_detail_invoice SET 	
			invoice_id = #{invoice},
			order_header_id = #{orderHeader},
			request_header_id = #{requestHeader}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM biz_detail_invoice
		WHERE id = #{id}
	</update>
	
</mapper>